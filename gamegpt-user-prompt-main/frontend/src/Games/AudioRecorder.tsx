const AudioRecorder = ({ onRecordingComplete }: { onRecordingComplete: (url: string) => void }) => {
  return (
    <div className="text-center">
      <p>Audio Recorder Placeholder</p>
    </div>
  );
};

export default AudioRecorder;
