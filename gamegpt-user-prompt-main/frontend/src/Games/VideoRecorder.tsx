const VideoRecorder = ({ onRecordingComplete }: { onRecordingComplete: (url: string) => void }) => {
  return (
    <div className="text-center">
      <p>Video Recorder Placeholder</p>
    </div>
  );
};

export default VideoRecorder;
